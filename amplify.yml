version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd front
        - npm ci
    build:
      commands: 
        - cd ../front
        - npm run build:$ENV --output-path=dist/front
  artifacts:
    baseDirectory: /front/dist/front/
    files:
      - '**/*'
  cache:
    paths:
    - node_modules/**/*
