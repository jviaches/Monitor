version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd front
        - npm ci
    build:
      commands: 
        - npm run build  
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
    - node_modules/**/*
